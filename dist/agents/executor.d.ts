import { Plan, Evidence } from '../core/types';
import { BudgetTracker } from '../core/budget';
export declare function runExecutor(plan: Plan, subQuestionIndex: number, runId: string, budget: BudgetTracker): Promise<Evidence[]>;
//# sourceMappingURL=executor.d.ts.map